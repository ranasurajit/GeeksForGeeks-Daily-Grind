class Solution {
    /**
     * Approach I : Using Array Sorting Approach
     * 
     * TC: O(N x log(N))
     * SC: O(1)
     */
    public void sort012(int[] arr) {
        Arrays.sort(arr); // TC: O(N x log(N))
    }
}
